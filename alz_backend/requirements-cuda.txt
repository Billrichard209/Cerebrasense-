# CUDA-enabled PyTorch wheels for RTX GPU training.
# Install this after requirements.txt when an NVIDIA GPU is available.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
